# Playwright 端到端测试用例列表

## 41个算法选择测试 (09-algorithm-selection.spec.ts)

### US1 - 任务节点自动匹配默认算法 (P1) - 7个测试

| # | 测试名称 | 测试步骤 |
|---|---------|---------|
| 1 | 拖拽MPC任务节点应自动匹配创建时间最新的MPC类型算法 | ① 打开页面 → ② 拖拽MPC节点到画布 → ③ 验证节点创建 → ④ 点击节点 → ⑤ 验证详情面板显示算法选择器 → ⑥ 验证算法名称显示 |
| 2 | 拖拽PSI任务节点应自动匹配PSI类型算法 | ① 打开页面 → ② 拖拽PSI节点到画布 → ③ 验证节点创建 → ④ 点击节点 → ⑤ 验证详情面板显示算法选择器 |
| 3 | 拖拽PIR任务节点应自动匹配PIR类型算法 | ① 打开页面 → ② 拖拽PIR节点到画布 → ③ 验证节点创建 → ④ 点击节点 → ⑤ 验证详情面板显示算法选择器（如存在） |
| 4 | 拖拽联邦学习任务节点应自动匹配FL类型算法 | ① 打开页面 → ② 拖拽FL节点到画布 → ③ 验证节点创建 → ④ 点击节点 → ⑤ 验证详情面板显示算法选择器（如存在） |
| 5 | 算法无参数模板时详情面板仅显示算法名称不显示参数表单 | ① 打开页面 → ② 拖拽MPC节点 → ③ 点击节点 → ④ 验证算法选择器显示 → ⑤ 检查参数表单（有则验证字段数≥0） |
| 6 | 侧边栏应显示所有计算任务节点 | ① 打开页面 → ② 验证侧边栏显示：MPC计算、PSI计算、PIR查询、联邦学习 |
| 7 | 侧边栏计算任务节点应该可拖拽 | ① 打开页面 → ② 验证MPC节点有`draggable="true"`属性 |

### US2 - 手动选择和更换算法 (P1) - 5个测试

| # | 测试名称 | 测试步骤 |
|---|---------|---------|
| 8 | 点击算法选择控件应显示该任务类型的所有可用算法列表 | ① 拖拽MPC节点 → ② 点击节点 → ③ 点击算法选择器 → ④ 验证下拉列表显示 → ⑤ 验证列表项数>0 |
| 9 | 算法列表仅显示与当前任务类型匹配的算法 | ① 拖拽MPC节点 → ② 点击节点 → ③ 点击算法选择器 → ④ 验证下拉列表显示 |
| 10 | 选择新算法后参数表单应更新 | ① 拖拽MPC节点 → ② 点击节点 → ③ 点击算法选择器 → ④ 如有多个算法，选择第二个 → ⑤ 验证算法名称更新 |
| 11 | 切换算法后已填写的参数值应被清空 | ① 拖拽MPC节点 → ② 点击节点 → ③ 获取当前算法名 → ④ 选择不同算法 → ⑤ 验证算法名称改变 |
| 12 | 点击下拉列表外部应关闭下拉 | ① 拖拽MPC节点 → ② 点击节点 → ③ 点击算法选择器打开下拉 → ④ 点击画布空白区域 → ⑤ 验证下拉关闭 |

### US3 - 录入和保存附加参数 (P2) - 6个测试

| # | 测试名称 | 测试步骤 |
|---|---------|---------|
| 13 | 字符串类型参数应显示文本输入框 | ① 拖拽MPC节点 → ② 点击节点 → ③ 检查参数表单 → ④ 验证文本输入框数量≥0 |
| 14 | 整数类型参数应显示数字输入框并支持范围验证 | ① 拖拽MPC节点 → ② 点击节点 → ③ 检查参数表单 → ④ 验证数字输入框数量≥0 |
| 15 | 布尔类型参数应显示复选框 | ① 拖拽MPC节点 → ② 点击节点 → ③ 检查参数表单 → ④ 验证复选框数量≥0 |
| 16 | 枚举类型参数应显示下拉选择框 | ① 拖拽MPC节点 → ② 点击节点 → ③ 检查参数表单 → ④ 验证下拉选择框数量≥0 |
| 17 | 导出JSON应包含算法配置和参数值 | ① 拖拽MPC节点 → ② 点击节点 → ③ 点击导出按钮 → ④ 验证按钮可见 |
| 18 | 必填参数验证应显示错误提示 | ① 拖拽MPC节点 → ② 点击节点 → ③ 检查参数表单 → ④ 验证必填标记数量≥0 |

### US4 - 查看和管理算法列表 (P2) - 6个测试

| # | 测试名称 | 测试步骤 |
|---|---------|---------|
| 19 | 应能通过设置菜单进入算法管理页面 | ① 打开页面 → ② 点击设置下拉 → ③ 点击算法管理 → ④ 验证页面标题 → ⑤ 验证返回按钮 → ⑥ 验证注册新算法按钮 |
| 20 | 算法管理页面应显示所有已注册算法的列表 | ① 进入算法管理页面 → ② 等待列表加载 → ③ 验证卡片数>0 → ④ 验证每个卡片显示名称和版本 |
| 21 | 算法类型过滤功能应正常工作 | ① 进入算法管理页面 → ② 获取初始数量 → ③ 选择MPC类型过滤 → ④ 验证过滤后数量≤初始数量 → ⑤ 重置验证恢复 |
| 22 | 关键字搜索功能应正常工作 | ① 进入算法管理页面 → ② 输入"SPDZ协议"搜索 → ③ 验证结果包含关键字 → ④ 清空搜索验证恢复 |
| 23 | 删除算法功能应显示确认对话框 | ① 进入算法管理页面 → ② 点击删除按钮 → ③ 验证确认对话框弹出 |
| 24 | 返回按钮应能返回流程编辑器 | ① 进入算法管理页面 → ② 点击返回按钮 → ③ 验证返回主页面 |

### US5 - 注册新算法 (P2) - 7个测试

| # | 测试名称 | 测试步骤 |
|---|---------|---------|
| 25 | 点击注册新算法按钮应显示算法注册表单 | ① 进入算法管理页面 → ② 点击注册新算法 → ③ 验证对话框显示 → ④ 验证表单字段（名称、英文名、版本、类型） |
| 26 | 表单验证：必填字段为空时应显示错误 | ① 点击注册新算法 → ② 直接点保存 → ③ 验证错误提示显示 → ④ 验证错误数>0 |
| 27 | 表单验证：英文名称格式验证 | ① 点击注册新算法 → ② 输入中文英文名 → ③ 点击保存 → ④ 验证英文名称错误提示 |
| 28 | 表单验证：版本号格式验证 | ① 点击注册新算法 → ② 输入"1.0"（缺v前缀） → ③ 点击保存 → ④ 验证版本号错误提示 |
| 29 | 成功注册新算法 | ① 点击注册新算法 → ② 填写完整表单（唯一名称+英文名+v1.0+MPC+描述） → ③ 点击保存 → ④ 搜索验证新算法显示 |
| 30 | 取消按钮应关闭对话框 | ① 点击注册新算法 → ② 点击取消按钮 → ③ 验证对话框关闭 |
| 31 | 点击对话框外部应关闭对话框 | ① 点击注册新算法 → ② 点击对话框外部 → ③ 验证对话框关闭 |

### US6 - 定义算法参数模板 (P3) - 7个测试

| # | 测试名称 | 测试步骤 |
|---|---------|---------|
| 32 | 应显示参数模板区域和添加参数按钮 | ① 点击注册新算法 → ② 验证参数模板区域 → ③ 验证添加参数按钮 |
| 33 | 点击添加参数按钮应显示参数配置表单 | ① 点击注册新算法 → ② 点击添加参数 → ③ 验证参数卡片 → ④ 验证字段（键名、标签、类型） |
| 34 | 参数类型选择器应显示所有支持的类型 | ① 点击注册新算法 → ② 点击添加参数 → ③ 验证类型下拉框 → ④ 验证有8种类型 → ⑤ 切换类型验证 |
| 35 | 应能添加多个参数 | ① 点击注册新算法 → ② 点击添加参数两次 → ③ 验证有2个参数卡片 |
| 36 | 应能删除参数 | ① 点击注册新算法 → ② 添加2个参数 → ③ 删除第一个 → ④ 验证只剩1个 |
| 37 | 参数必填复选框应可切换 | ① 点击注册新算法 → ② 点击添加参数 → ③ 点击必填复选框 → ④ 验证勾选状态 → ⑤ 再次点击验证取消 |
| 38 | 带参数模板的算法注册成功后应在列表中显示参数数量 | ① 点击注册新算法 → ② 填写基本信息 → ③ 添加1个参数 → ④ 保存 → ⑤ 搜索验证显示"1 个"参数 |

### 边缘情况和跨功能测试 - 3个测试

| # | 测试名称 | 测试步骤 |
|---|---------|---------|
| 39 | 无可用算法时应显示空状态提示 | ① 拖拽MPC节点 → ② 点击节点 → ③ 验证算法区域显示算法信息或空状态 |
| 40 | 设置下拉菜单应正确工作 | ① 打开页面 → ② 点击设置按钮 → ③ 验证下拉菜单显示 → ④ 验证算法管理菜单项 |
| 41 | 页面刷新后应能正常重新加载 | ① 刷新页面 → ② 验证vue-flow组件可见 → ③ 验证flow-editor组件可见 |

---

## 37个DAG编排测试 (10-dag-orchestration.spec.ts)

### 基础场景 - 数据源节点创建 - 3个测试

| # | 测试名称 | 测试步骤 |
|---|---------|---------|
| 1 | 创建数据库表数据源节点 | ① 打开页面启用测试模式 → ② 拖拽"数据库表"节点到画布(200,100) → ③ 验证节点数=1 |
| 2 | 创建CSV文件数据源节点 | ① 打开页面启用测试模式 → ② 拖拽"CSV 文件"节点到画布(200,100) → ③ 验证节点数=1 |
| 3 | 创建实时数据源节点 | ① 打开页面启用测试模式 → ② 拖拽"实时数据源"节点到画布(200,100) → ③ 验证节点数=1 |

### 基础场景 - 计算任务节点创建 - 4个测试

| # | 测试名称 | 测试步骤 |
|---|---------|---------|
| 4 | 创建PSI计算任务节点 | ① 打开页面启用测试模式 → ② 拖拽"PSI 计算"节点到画布(200,200) → ③ 验证节点数=1 |
| 5 | 创建PIR查询任务节点 | ① 打开页面启用测试模式 → ② 拖拽"PIR 查询"节点到画布(200,200) → ③ 验证节点数=1 |
| 6 | 创建MPC计算任务节点 | ① 打开页面启用测试模式 → ② 拖拽"MPC 计算"节点到画布(200,200) → ③ 验证节点数=1 |
| 7 | 创建联邦学习任务节点 | ① 打开页面启用测试模式 → ② 拖拽"联邦学习"节点到画布(200,200) → ③ 验证节点数=1 |

### 基础场景 - 本地任务节点创建 - 2个测试

| # | 测试名称 | 测试步骤 |
|---|---------|---------|
| 8 | 创建本地结果处理节点 | ① 打开页面启用测试模式 → ② 拖拽"本地结果处理"节点到画布(200,200) → ③ 验证节点数=1 |
| 9 | 创建本地Query节点 | ① 打开页面启用测试模式 → ② 拖拽"本地Query"节点到画布(200,200) → ③ 验证节点数=1 |

### 简单流程 - 数据源到计算任务 - 3个测试

| # | 测试名称 | 测试步骤 |
|---|---------|---------|
| 10 | 数据库表 -> PSI计算 简单流程 | ① 拖拽数据库表节点(150,100) → ② 拖拽PSI节点(150,250) → ③ 验证节点数=2 |
| 11 | CSV文件 -> MPC计算 简单流程 | ① 拖拽CSV节点(150,100) → ② 拖拽MPC节点(150,250) → ③ 验证节点数=2 |
| 12 | 实时数据源 -> PIR查询 简单流程 | ① 拖拽实时数据源节点(150,100) → ② 拖拽PIR节点(150,250) → ③ 验证节点数=2 |

### 多数据源流程 - 3个测试

| # | 测试名称 | 测试步骤 |
|---|---------|---------|
| 13 | 两个数据源 -> PSI计算 | ① 拖拽数据库表(100,100) → ② 拖拽数据库表(250,100) → ③ 拖拽PSI(175,280) → ④ 验证节点数=3 |
| 14 | 数据库表 + CSV -> MPC计算 | ① 拖拽数据库表(100,100) → ② 拖拽CSV(250,100) → ③ 拖拽MPC(175,280) → ④ 验证节点数=3 |
| 15 | 三个数据源 -> MPC计算 | ① 拖拽数据库表(50,100) → ② 拖拽CSV(175,100) → ③ 拖拽数据库表(300,100) → ④ 拖拽MPC(175,280) → ⑤ 验证节点数=4 |

### 计算任务串联流程 - 3个测试

| # | 测试名称 | 测试步骤 |
|---|---------|---------|
| 16 | PSI -> MPC 串联流程 | ① 拖拽数据库表(100,50) → ② 拖拽数据库表(250,50) → ③ 拖拽PSI(175,180) → ④ 拖拽MPC(175,320) → ⑤ 验证节点数=4 |
| 17 | MPC -> 本地结果处理 串联流程 | ① 拖拽数据库表(100,50) → ② 拖拽CSV(250,50) → ③ 拖拽MPC(175,180) → ④ 拖拽本地结果处理(175,320) → ⑤ 验证节点数=4 |
| 18 | PSI -> MPC -> 本地Query 三级串联 | ① 拖拽数据库表(100,30) → ② 拖拽数据库表(250,30) → ③ 拖拽PSI(175,140) → ④ 拖拽MPC(175,260) → ⑤ 拖拽本地Query(175,380) → ⑥ 验证节点数=5 |

### 复杂DAG流程 - 3个测试

| # | 测试名称 | 测试步骤 |
|---|---------|---------|
| 19 | 双分支流程 | ① 拖拽数据库表(80,100) → ② 拖拽PSI(80,250) → ③ 拖拽CSV(270,100) → ④ 拖拽MPC(270,250) → ⑤ 验证节点数=4 |
| 20 | 汇聚流程（7节点） | ① 拖拽数据库表(50,50) → ② 拖拽数据库表(200,50) → ③ 拖拽PSI(125,180) → ④ 拖拽CSV(300,50) → ⑤ 拖拽CSV(450,50) → ⑥ 拖拽MPC(375,180) → ⑦ 拖拽本地结果处理(250,320) → ⑧ 验证节点数=7 |
| 21 | 菱形流程 | ① 拖拽数据库表(200,50) → ② 拖拽PSI(100,180) → ③ 拖拽MPC(300,180) → ④ 拖拽本地结果处理(200,320) → ⑤ 验证节点数=4 |

### PIR特殊流程 - 2个测试

| # | 测试名称 | 测试步骤 |
|---|---------|---------|
| 22 | PIR查询需要普通数据源和实时数据源 | ① 拖拽数据库表(100,100) → ② 拖拽实时数据源(250,100) → ③ 拖拽PIR(175,250) → ④ 验证节点数=3 |
| 23 | PIR -> 本地结果处理流程 | ① 拖拽数据库表(100,50) → ② 拖拽实时数据源(250,50) → ③ 拖拽PIR(175,180) → ④ 拖拽本地结果处理(175,320) → ⑤ 验证节点数=4 |

### 联邦学习流程 - 2个测试

| # | 测试名称 | 测试步骤 |
|---|---------|---------|
| 24 | 联邦学习基本流程 | ① 拖拽数据库表(100,100) → ② 拖拽数据库表(250,100) → ③ 拖拽FL(175,250) → ④ 验证节点数=3 |
| 25 | 联邦学习 -> 本地Query流程 | ① 拖拽CSV(100,50) → ② 拖拽CSV(250,50) → ③ 拖拽FL(175,180) → ④ 拖拽本地Query(175,320) → ⑤ 验证节点数=4 |

### 大规模流程 - 3个测试

| # | 测试名称 | 测试步骤 |
|---|---------|---------|
| 26 | 5个节点的中等规模流程 | ① 拖拽3个数据源 → ② 拖拽PSI → ③ 拖拽MPC → ④ 验证节点数=5 |
| 27 | 6个节点的完整流程 | ① 拖拽4个数据源 → ② 拖拽PSI → ③ 拖拽MPC → ④ 验证节点数=6 |
| 28 | 8个节点的大型流程 | ① 拖拽4个数据源 → ② 拖拽PSI → ③ 拖拽MPC → ④ 拖拽本地结果处理 → ⑤ 拖拽FL → ⑥ 验证节点数=8 |

### 边缘情况 - 4个测试

| # | 测试名称 | 测试步骤 |
|---|---------|---------|
| 29 | 只创建数据源节点不创建计算任务 | ① 拖拽数据库表(200,100) → ② 拖拽CSV(200,250) → ③ 验证节点数=2 |
| 30 | 只创建计算任务节点不创建数据源 | ① 拖拽MPC(200,200) → ② 验证节点数=1 |
| 31 | 多个相同类型的节点 | ① 拖拽3个数据库表节点 → ② 验证节点数=3 |
| 32 | 混合所有类型节点 | ① 拖拽数据库表 → ② 拖拽实时数据源 → ③ 拖拽PSI → ④ 拖拽PIR → ⑤ 拖拽本地结果处理 → ⑥ 验证节点数=5 |

### 节点操作 - 2个测试

| # | 测试名称 | 测试步骤 |
|---|---------|---------|
| 33 | 点击节点应显示详情面板 | ① 拖拽数据库表节点 → ② 点击节点 → ③ 验证详情面板可见 |
| 34 | 创建节点后画布应正确缩放 | ① 拖拽数据库表节点 → ② 拖拽MPC节点 → ③ 验证画布可见 → ④ 验证节点数=2 |

### 页面状态 - 3个测试

| # | 测试名称 | 测试步骤 |
|---|---------|---------|
| 35 | 页面刷新后画布应清空 | ① 拖拽数据库表节点 → ② 验证节点数=1 → ③ 刷新页面 → ④ 验证节点数=0 |
| 36 | 未保存提示应显示 | ① 拖拽数据库表节点 → ② 验证"未保存"标识可见 |
| 37 | 侧边栏应正确显示所有节点类型 | ① 验证"数据源"可见 → ② 验证"计算任务"可见 → ③ 验证"计算模型"可见 → ④ 验证"本地计算任务"可见 |

---

## 统计

- **算法选择测试**: 41个
- **DAG编排测试**: 37个
- **总计**: 78个测试用例
